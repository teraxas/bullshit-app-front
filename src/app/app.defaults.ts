import { MatDialogConfig } from '@angular/material/';

export class MDDefaults {
    static readonly MD_DIALOG_CFG: MatDialogConfig = {
      // height: '500px',
      // width: '360px',
      disableClose: true
    };
}
